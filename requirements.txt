# ============================================================
# Predictive Maintenance System — Python Dependencies
# ============================================================
# Python 3.11 | Windows / Linux
#
# INSTALL:
#   python -m venv venv
#   .\venv\Scripts\activate          # Windows
#   pip install --upgrade pip setuptools wheel
#   pip install -r requirements.txt
# ============================================================

# ── Core ML ──────────────────────────────────────────────────
numpy>=1.26.0,<2.0
pandas>=2.1.0,<3.0
scikit-learn>=1.3.0,<2.0
scipy>=1.11.0,<2.0
joblib>=1.3.0,<2.0
imbalanced-learn>=0.11.0,<1.0

# ── API Layer ────────────────────────────────────────────────
fastapi>=0.104.0,<1.0
uvicorn[standard]>=0.24.0,<1.0
pydantic>=2.5.0,<3.0
pydantic-settings>=2.1.0,<3.0
python-multipart>=0.0.6,<1.0
httpx>=0.25.0,<1.0

# ── Experiment Tracking ──────────────────────────────────────
mlflow>=2.9.0,<3.0

# ── Monitoring ───────────────────────────────────────────────
prometheus-client>=0.19.0,<1.0

# ── Frontend ─────────────────────────────────────────────────
streamlit>=1.29.0,<2.0
plotly>=5.18.0,<6.0
requests>=2.31.0,<3.0

# ── Data & Model Versioning ─────────────────────────────────
# dvc[all] includes: S3, GDrive, GCS, Azure, SSH, HDFS, WebDAV
# No need to install dvc-s3, dvc-gdrive etc. separately
dvc[all]>=3.55.0,<4.0
# Pin these to fix known Windows errors:
#   "cannot import name '_DIR_MARK'"   → pathspec too old
#   "'Repo' object has no attribute 'stage'" → scmrepo too old
pathspec>=0.12.1
scmrepo>=3.3.0
gitpython>=3.1.40

# ── Configuration ────────────────────────────────────────────
pyyaml>=6.0.1,<7.0
python-dotenv>=1.0.0,<2.0

# ── Testing ──────────────────────────────────────────────────
pytest>=7.4.0,<9.0
pytest-cov>=4.1.0,<6.0

# NOTE: Apache Airflow is NOT installed locally.
# It runs inside Docker only (see docker/airflow/Dockerfile).